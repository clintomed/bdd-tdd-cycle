-# in app/views/movies/similar.html.haml

%h2 Movie like #{movie.title}

%p#description= @movie.description

%h3 Movies by same director:
- if @sameDirector
  %ul
    - @sameDirector.each do |movie|
      %li
        %td= link_to "#{movie.title}", movie_path(movie)
